import { NextPage } from "next";

import { styled } from "../stitches.config";

const Page: NextPage = () => {
  return <p>Hello World! こんにちは</p>;
};

export default Page;

const StyledDiv = styled("div", {});
